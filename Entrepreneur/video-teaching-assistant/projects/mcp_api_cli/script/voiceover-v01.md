# Voiceover v01

本文件是 `SCRIPT.md` 的配音切分版。每一条是完整语义句，后续 Fish Audio 配音和词级对齐以此为唯一文本源。

| ID | 时间段 | 配音句 |
|---|---:|---|
| S01-01 | 0:00–0:08 | 你是不是也遇到过这种情况：刚开始做 Agent，就被一堆英文词绕晕。 |
| S01-02 | 0:08–0:16 | 它们看起来都和调用工具有关，但其实根本不在同一层。 |
| S02-01 | 0:16–0:28 | 先看总地图：上面是用户和 Agent 应用，下面是 ADK，负责组织模型、工具、状态和流程。 |
| S02-02 | 0:28–0:40 | 再往下是 LLM 和 Tool Calling，最底层才是 API、SDK、CLI，以及 MCP 连接的外部系统。 |
| S03-01 | 0:40–0:51 | API 解决软件 A 怎么调用软件 B；SDK 把认证、重试和错误处理封装起来。 |
| S03-02 | 0:51–1:03 | CLI 则是终端入口，所以这三个词通常不是竞争关系，而是同一服务的三种使用方式。 |
| S04-01 | 1:03–1:16 | Function Calling 让模型产生结构化请求，但真正执行函数的仍然是应用程序。 |
| S04-02 | 1:16–1:28 | Tool Calling 是更大的总称，Function、搜索和 MCP 工具都可以属于 Tool。 |
| S05-01 | 1:28–1:42 | 没有 MCP 时，每接一个外部系统，都要重写认证、Schema、调用和错误处理。 |
| S05-02 | 1:42–1:55 | MCP 统一了连接方式，AI 应用通过 Client 连接 Server，发现 Tools、Resources 和 Prompts。 |
| S06-01 | 1:55–2:08 | MCP 往往仍然调用后面的 API、SDK、CLI、数据库或文件系统。 |
| S06-02 | 2:08–2:20 | ADK 在更上面，负责 Agent 的状态、工作流、回调、多 Agent 协作和评测。 |
| S07-01 | 2:20–2:35 | 用户要查订单，ADK 组织流程，LLM 通过 Tool Calling 选择查询工具。 |
| S07-02 | 2:35–2:50 | 工具再通过 MCP、SDK 或 API 读取数据库，返回结果后继续退款。 |
| S08-01 | 2:50–3:02 | API 是软件接口，SDK 方便开发，CLI 提供命令行入口。 |
| S08-02 | 3:02–3:14 | Function Calling 是结构化请求，Tool Calling 是总机制，MCP 负责统一连接，ADK 负责组织 Agent。 |
| S09-01 | 3:14–3:30 | 以后先问它属于哪一层、连接的是谁、解决的是调用、连接，还是编排。 |

